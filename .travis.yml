language: cpp

compiler:
    - clang
    - gcc

os:
    - linux
    - os

before_script: 
    - sudo apt get update 
    - sudo apt get install -y libxm12-dev

script:
    - qmake -project "CONFIG += qtestlib"
    - qmake Word-Flow/Word-Flow.pro
    - make
    - cd build



